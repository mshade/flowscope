name: PR Comment
on:
  pull_request_target:
    types: [opened]

jobs:
  comment:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - run: echo "no write access — safe"
